# Core AI & Vision
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Tracking Math (Required for OCSORT)
scipy>=1.10.0
filterpy>=1.4.5
scikit-learn>=1.3.0

# Web Application (Orchestrator)
flask>=2.3.0
flask-socketio>=5.3.0
eventlet>=0.33.0

# Hardware (RPi Specific - acts as placeholder on PC)
# Note: picamera2 is often a system package on RPi, but we list it here
# to attempt install or fail gracefully.
picamera2; platform_machine == "aarch64"