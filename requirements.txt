# Minimart 2.0 - Jetson Orin Nano Edition
# Python Dependencies

# Web Framework
flask>=2.3.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
python-socketio>=5.8.0
python-engineio>=4.5.0

# Async Support
eventlet>=0.33.0
gevent>=23.0.0

# Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0

# Object Detection (CPU fallback)
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Jetson-specific (install via pip3 on Jetson)
# Note: TensorRT and PyCUDA are typically pre-installed with JetPack
# jetson-stats  # sudo pip3 install jetson-stats

# JSON/Data
dataclasses-json>=0.5.7

# HTTP Client (for remote detector option)
requests>=2.28.0

# Optional: Database
# psycopg2-binary>=2.9.0
# sqlalchemy>=2.0.0

# Development
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0

# =============================================================================
# JETSON-SPECIFIC INSTALLATION NOTES
# =============================================================================
#
# 1. TensorRT & PyCUDA (pre-installed with JetPack 5.x)
#    - Verify: python3 -c "import tensorrt; print(tensorrt.__version__)"
#    - If missing: Follow NVIDIA docs for your JetPack version
#
# 2. jetson-stats (for telemetry)
#    sudo pip3 install jetson-stats
#    sudo systemctl restart jtop
#
# 3. PyTorch for Jetson (if not using system version)
#    - Download wheel from NVIDIA forums for your JetPack version
#    - pip3 install torch-2.x.x-cp3xx-linux_aarch64.whl
#
# 4. OpenCV with CUDA (optional, for faster preprocessing)
#    - Usually pre-installed with JetPack
#    - Verify: python3 -c "import cv2; print(cv2.getBuildInformation())" | grep CUDA
#
# =============================================================================
